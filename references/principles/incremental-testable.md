# Incremental and Testable Delivery

> Every step must produce something independently verifiable.

## Rules

- one step, one verifiable increment
- do not stack multiple large edits before validation
- every step needs an explicit validation path
- if the change cannot be validated in the current step, the step is not ready yet
