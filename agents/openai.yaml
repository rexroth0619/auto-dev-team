interface:
  display_name: "auto-dev-team"
  short_description: "Structured coding workflow with semantic navigation, multi-scale planning, verification, resume, and rollback guardrails"
  default_prompt: "Use $auto-dev-team for coding, debugging, refactoring, testing, project survey, and safe write workflows with semantic code navigation, multi-scale planning, context recovery, verification, and rollback guardrails."

policy:
  allow_implicit_invocation: true
